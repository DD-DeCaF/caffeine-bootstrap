os:
- linux
language: shell

git:
  depth: 2

branches:
  only:
  - master

services:
- docker

before_install:
- make check
- make setup

install:
- make install

script:
- make initialize

notifications:
  email: false
